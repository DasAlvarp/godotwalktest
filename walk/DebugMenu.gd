extends Control

# class member variables go here, for example:
# var a = 2
# var b = "textvar"

func _draw():
	#var pos = get_node("Char").get_pos()
	#get_node("PosInfo").set_text(str(pos.x) + ", " + str(pos.y))